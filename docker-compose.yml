services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./artifacts:/app/artifacts
      - ./data:/app/data
    environment:
      - INFRASENTINEL_MODEL_PATH=/app/artifacts/models/anomaly_model.joblib
